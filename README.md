# homelessSPEAKERcorner
speaker corner for homeless debate in your NEIGHBOURhood

## Running locally
The site is static. Serve the folder with any local HTTP server (for example `npx serve .` or `python -m http.server 4173`), then browse to `/index.html`.
